# Project rules

- **No em dashes.** Do not use em dashes (—). Use a colon, period, comma, or rephrase instead (e.g. "X. Y" or "X: Y" instead of "X—Y"). In titles that separate the post name from the site name, use a pipe: `Title | LUMP Depot`.
